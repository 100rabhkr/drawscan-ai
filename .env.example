# DrawScan AI — Configuration
# No external API keys required. All processing runs locally.

# Optional: Database path (default: data/drawscan.db)
DB_PATH=data/drawscan.db

# Optional: Upload/output directories
UPLOAD_DIR=uploads
OUTPUT_DIR=outputs

# Optional: Max file size in MB
MAX_FILE_SIZE_MB=20
